let multer,path;_5f4‍.x([["uploadPicture",()=>uploadPicture]]);_5f4‍.w("multer",[["default",["multer"],function(v){multer=v}]]);_5f4‍.w("path",[["default",["path"],function(v){path=v}]]);


const storage = multer.diskStorage({
  destination: (req, file, cb) => {
    cb(null, path.join(__dirname, "../uploads"));
  },
  filename: (req, file, cb) => {
    cb(null, `${Date.now()}-${file.originalname}`);
  },
});

const uploadPicture = multer({
  storage: storage,
  limits: {
    fileSize: 3 * 1000000, // 3MB
  },
  fileFilter: function (req, file, cb) {
    let ext = path.extname(file.originalname);
    if (ext !== ".png" && ext !== ".jpg" && ext !== ".jpeg") {
      return cb(new Error("Sólo se permiten imágenes"));
    }
    cb(null, true);
  },
});


